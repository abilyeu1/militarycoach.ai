import Support from "@/view/support";
import Head from "next/head";
import React, { Fragment } from "react";

const SupportPage = () => {
  return (
    <Fragment>
      <Head>
        <title>militarycoach.ai | Support</title>
      </Head>

      <Support />
    </Fragment>
  );
};

export default SupportPage;
